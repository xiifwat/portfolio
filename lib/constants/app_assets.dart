class AppAssets {
  // Images
  static const profilePicture = 'assets/profile_picture.jpeg';
  
  // Icons (if you add custom icons later)
  static const iconPath = 'assets/icons/';
  
  // Fonts (already configured in pubspec.yaml)
  static const fontFamily = 'Montserrat';
}